import React, { useContext } from 'react';
import { LoginContext } from '../../App';

const About = () => {
    return (
        <div>
            I am About
        </div>
    );
};

export default About;